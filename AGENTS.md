# AGENTS.md file

## Contexts
- This is a rule for textlint.
- Testlint is a linting tool for natural luangage.
- Hot to implement a textlint rule can be refer by fetching `https://textlint.org/docs/rule#setup-for-creating-rule`

## Dev environment tips
- npx create-textlint-rule was already executed to setup dev environment.

## Testing instructions
- Run `npm test` to run every check defined for that package.

## PR instructions
- Always run `npm test` before committing.